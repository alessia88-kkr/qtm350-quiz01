##all commands
git clone https://github.com/alessia88-kkr/qtm350-quiz01.git
cd qtm350-quiz01
touch commands.txt
git checkout -b feature-update
mkdir reports
touch reports/summary.md
echo "This document provides summary of the project updates." > reports/summary.md
git add reports
git commit -m "Add reports directory"
touch reports/{file1,file2,file3}.txt
git mv data/raw-data.csv data/input-data.csv
mkdir scripts/backup && cp scripts/*.py scripts/backup/
git rm docs/documentation.md
git add -A && git commit -m "Update project files"
touch .gitignore
echo -e "temp/\ndataset0?.csv" > .gitignore
printf "temp/\ndataset0?.csv\n" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file"
git checkout main && git merge feature-update
git push origin main
nano commands.txt
